{
  "name": "static-site-multiversion",
  "version": "0.1.0",
  "private": true,
  "description": "Tooling at the repo root: Playwright for screenshot verification of the demo flow. Each examples/<builder>/ owns its own package.json for the actual site build.",
  "scripts": {
    "screenshot": "scripts/screenshot.sh"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}

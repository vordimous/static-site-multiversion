---
home: true
heroText: Versioned VuePress Example
tagline: Vue-based docs, the source pattern this repo grew out of.
actions:
  - text: Read the guide
    link: /guide/getting-started.html
    type: primary
---

This example is the workflow this repo distilled. The original multi-version flow lived inside a VuePress 2 docs site, and that workflow became the env-var contract every example here honors. The current version is set at build time via the `SITE_VERSION_KEY` environment variable.

Demo build marker: **next**

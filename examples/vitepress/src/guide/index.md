# Guide

This is the guide page for the Versioned VitePress Example. It exists so the build covers a non-homepage route under `/<key>/guide/`, which is what the static-site-multiversion contract expects every example to verify.

Cross-link back to the [home page](/).
